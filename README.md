# todo-reaed
this is a simple todo appliaction built with react - this app allows users to add and remove tasks easily
FEATURES- in this tod i added features like , add new todo, remove todos , instant UI update using react state
How It Works
Uses useState to manage:
todo → list of tasks
list → input field value
addTodo() → adds new task
remove(index) → deletes selected task
